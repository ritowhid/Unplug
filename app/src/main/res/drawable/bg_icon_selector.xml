<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected (active tab) -->
    <item android:state_checked="true" android:drawable="@drawable/bg_icon_selected" />

    <!-- Pressed (when touched) -->
    <item android:state_pressed="true" android:drawable="@drawable/bg_icon_pressed" />

    <!-- Default (unselected) -->
<!--    <item android:drawable="@drawable/bg_icon_default" />-->
</selector>
